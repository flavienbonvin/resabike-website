System admin
Zone admin
Bus driver

var Role = require('./objects/Role');
var roles = [];

roles.push(new Role(1, 'System admin').convertToSequelize(), new Role(2, 'Zone admin').convertToSequelize(), new Role(3, 'Bus driver').convertToSequelize())

db.Role.bulkCreate(roles).then(() => {
    
})